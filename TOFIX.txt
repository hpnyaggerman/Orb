- [Post-process] Display the refine pass in the Inspector UI. Rework this view to avoid displaying excessive information.
- [Post-process] Detect structural repetition across multiple paragraphs, not just individual sentences.
- [Post-process] Implement phrase repetition detection as well, not just anti-slop detection. Tell the model to write about something else entirely for this sentence.

- Improvement: Consider always sending lengths if length_guard is enabled? => No need to send refine_rewrite tool. Or considering always sending refine_rewrite tool?
=> Have a checkbox for this in Length Guard

- Feature: Support showing reasoning in the UI for all passes (director, writer, refiner). It should be its own collapsable arrow section in the Inspector panel. It is a box, and the height is fixed new reasoning tokens will be streamed to it and the box will automatically scroll down as new tokens arrive.
- Feature: User-defined fields for the director to consider (e.g. physicality).
- Feature: Reasoning toggle for writer pass - for when endpoint supports alternating reasoning.
- Feature: Implement min_words for length_guard in refine pass.
- Feature: Explore using logit bias during the writer pass to prevent the assistant from outputting the tool call token. Add a "test" button to identify the `tool_call` token. Save this token keyed by the model+endpoint combination, so it can be banned in subsequent writer passes for that same combination.

-- Not yet --
- Feature: Add a magic button to the AI's message for "rewrite with custom direction." Clicking it opens a small input box where the user can specify the rewrite direction.
- Feature: During and after the refine pass, the latest AI message box should indicate which phrases were changed. Highlight the changed sentences and display a tooltip showing the original text when the cursor hovers over them.
- Feature: Add a hash prefix to guarantee KV cache reuse.
