{
  "name": "orb",
  "private": true,
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@evilmartians/lefthook": "^2.1.9"
  },
  "scripts": {
    "prepare": "lefthook install",
    "format:frontend": "biome format frontend/ --write",
    "format:backend": "bash scripts/format_backend.sh",
    "lint": "bash scripts/lint.sh && biome check frontend/",
    "test": "python3 -m pytest tests/"
  }
}
